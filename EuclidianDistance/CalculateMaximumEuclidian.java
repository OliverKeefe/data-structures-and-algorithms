class Solution {
    public int robotSim(int[] commands, int[][] obstacles) {

        int[][] directions = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};
        int[] coords;
        for (command : commands) {
            if (command )
        }
        
        // Store the current pos of the robit
        // 



    }
}
